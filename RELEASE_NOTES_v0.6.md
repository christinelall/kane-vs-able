# v0.6

Documentation-only release for submission clarity.

Changes:
- Added an explicit Quick Start section.
- Clarified that Kane CLI and Codex CLI are external prerequisites.
- Added installation and authentication commands for both CLIs.
- Clarified that `npm start` starts the application but does not install or authenticate external agents.
- Kept the one-command claim scoped correctly: once prerequisites are configured, the app runs with `npm start`.
